language: php
php:
  - 7.0

mysql:
  database: drupal
  username: root
  encoding: utf8
  
sudo: false

before_install:
  - phpenv config-rm xdebug.ini
  - composer --verbose self-update
  - composer --version

install:
  - composer --verbose install
  # Create MySQL Database
  - mysql -e 'create database drupal;'
  - export SIMPLETEST_DB=mysql://root:@127.0.0.1/drupal
  # Remove our included settings.php
  - rm -rf $TRAVIS_BUILD_DIR/web/sites/default/settings.php


script:
  # Test stable release
  - cd $TRAVIS_BUILD_DIR/web
  # Install drupal default profile
  - ./../vendor/bin/drush --verbose site-install testing --db-url=mysql://root:@127.0.0.1/drupal --yes
  - php core/scripts/run-tests.sh PHPUnit db-url=mysql://root:@127.0.0.1/drupal --yes
  - ./../vendor/bin/drush status
  - ./../vendor/bin/drupal

